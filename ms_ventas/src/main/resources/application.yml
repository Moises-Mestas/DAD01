spring:
  application:
    name: ms-ventas-service
  config:
    import: optional:configserver:http://localhost:7070  # Aseg�rate de que esta URL est� bien configurada
  profiles:
    active: development

